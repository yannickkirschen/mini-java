package de.dhbw.compiler.parser;
/*
import de.dhbw.compiler.ast.expressions.LocalOrFieldVar;
import de.dhbw.compiler.ast.stmtexprs.Assign;
import de.dhbw.compiler.parser.antlr.MinijavaParser;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class StatementExpressionGeneratorTest {

    StatementExpressionGenerator statementExpressionGenerator;
    ExpressionGenerator expressionGenerator;

    @BeforeEach
    void setup(){
        statementExpressionGenerator = new StatementExpressionGenerator();
        expressionGenerator = new ExpressionGenerator();
    }

    @Test
    void visitAssign(){
        //Id != null
        MinijavaParser.AssignableContext assignableContext = new MinijavaParser.AssignContext(new Assign(null,null));
        assertEquals(new Assign(new LocalOrFieldVar("id"),expressionGenerator.visit( assignableContext.expr() ) ),)
    }
}
*/
